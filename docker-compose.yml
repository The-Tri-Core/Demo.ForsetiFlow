services:
  project-manager:
    build: .
    container_name: project-manager
    user: "${UID}:${GID}"
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/instance
    restart: unless-stopped
